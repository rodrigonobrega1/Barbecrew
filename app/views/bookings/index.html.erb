
<h3 class="mt-3 mb-4 text-center">My Barbecues bookings</h3>

<div class="container">

    <div class="row">
        <% @bookings.each do |booking| %>
            <div class="col-md-2">
                <div class="flat-card3">
                    <div class="flat-card-image" style="background-image: url('<%= booking.barbecue.picture %>')"></div>
                    <div class="flat-card-content2">
                        <div class="flat-card-title">
                            <h6><%= booking.barbecue.barbecue_model %></h6>
                            <p>Date in: <%= booking.date_in %></p>
                            <p>Date out: <%= booking.date_out %></p>
                        </div>
                        <div class="flat-card-actions">
                        <div class="btn-group btn-sm" role="group" aria-label="Button group">
                            <%= link_to 'Delete', booking_path(booking), class: "btn btn-outline-danger btn-sm" %>
                        </div>
                        </div>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>




<h3 class="mt-3 mb-4 text-center">My booked</h3>

<div class="container">
    <div class="row">
        <% @booked.each do |booking| %>
            <div class="col-md-2">
                <div class="flat-card3">
                    <div class="flat-card-image" style="background-image: url('<%= booking.barbecue.picture %>')"></div>
                    <div class="flat-card-content2">

                            <h6><%= booking.barbecue.barbecue_model %></h6>
                            <p>Date in: <%= booking.date_in %></p>
                            <p>Date out: <%= booking.date_out %></p>

                        <div class="flat-card-actions">
                        <div class="btn-group btn-sm" role="group" aria-label="Button group">
                            <%= link_to 'Delete', booking_path(booking), class: "btn btn-outline-danger btn-sm" %>
                        </div>
                        </div>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>